<Page
    x:Class="TestBed.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestBed"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vtb="using:ValidatingTextBox"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="10">
            <vtb:ValidatingTextBox>
                <vtb:ValidationPair ErrorMessage="I'm invalid!" ValidationFunction="{x:Bind ValidateFunction}"/>
                <vtb:ValidationPair ErrorMessage="Exclamation marks not allowed either >:|" ValidationFunction="{x:Bind NoExclamationsFunction}"/>
            </vtb:ValidatingTextBox>
            <vtb:ValidatingTextBox x:Name="CodeBehindBox"/>
        </StackPanel>
    </Grid>
</Page>

